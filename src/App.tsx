import styles from "@/App.module.css"; // Import the CSS module

function App() {
  return (
    <div className={styles.container}>
      <p className={styles.text}>foweifnwoeinf</p>
    </div>
  );
}

export default App;
